export default function Navbar() {
  return (
    <div style={styles.nav}>
      <h2>
        Welcome Admin 👋
      </h2>
    </div>
  );
}

const styles = {
  nav: {
    background: "#1e293b",
    padding: "20px",
    borderRadius: "16px"
  }
};